{
  "rules": {
    "users": {
      "$user_id": {
        ".read":  "$user_id === auth.uid",
        ".write": "$user_id === auth.uid"
      }
    },
    "reports": {
      ".read":  "auth != null",
      ".write": "auth != null"
    }
  }
}