# Specifies intentionally untracked files to ignore when using Git
# http://git-scm.com/docs/gitignore

node_modules/
.idea/

# Firebase CLI local deploy cache (machine-local, regenerated on each deploy).
.firebase/

